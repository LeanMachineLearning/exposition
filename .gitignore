/.lake
# `JunkValues/` is a nested Lake package with a build directory of its own. Its (empty) manifest is
# committed, so that a project requiring it from git does not get a "missing manifest" warning.
/JunkValues/.lake
/_out/
/*-out*/
/dist/
