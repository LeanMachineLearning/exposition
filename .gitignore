/.lake
# `LeanSpec/` and `LeanDeps/` are nested Lake packages with build directories of their own. Their
# (empty) manifests are committed, so that a project requiring one from git does not get a
# "missing manifest" warning.
/LeanSpec/.lake
/LeanDeps/.lake
/_out/
/*-out*/
/dist/
